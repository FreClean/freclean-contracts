{
  "name": "freclean-contracts",
  "version": "0.1.0",
  "description": "FreClean's single smart contract: an agreement/payment-compliance registry for multi-year B2B service and supply contracts on Celo. No token, no fund custody.",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:alfajores": "hardhat run deploy.js --network celoAlfajores",
    "gen:contracts-docx": "node generate-contract-docs.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6"
  },
  "dependencies": {
    "docx": "^8.5.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
